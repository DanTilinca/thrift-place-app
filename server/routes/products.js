import express from 'express';
const router = express.Router();

router.get('/', (req, res) => {
  // Fetch all products
});

router.post('/', (req, res) => {
  // Create a new product
});

export default router;
